# Server Utilities

Helper functions for the backend.

## Files
- `scraper.js`: Contains the logic to fetch external URLs and parse their HTML to extract metadata (Title, Description, Image) using `axios` and `cheerio`.
